matches:
  - trigger: 'log;'
    replace: 'console.log("$|$")'

  - trigger: 'err;'
    replace: 'console.error("$|$")'

  - trigger: 'trace;'
    replace: 'console.trace($|$)'

  - trigger: 'profile;'
    replace: |-
      console.profile("time");
      console.profileEnd("time");

  - trigger: 'time;'
    replace: |-
      console.time("time");
      console.timeEnd("time");

  - trigger: 'cc;'
    replace: 'contentview/index.vue'

  - trigger: 'mm;'
    replace: '检查 mcp-relationships.json 和 capability-packs 目录下的能力包是否有需要更新'

  - trigger: 'ck;'
    replace: '整体再检查一遍，确保没有遗漏修改和遗漏修改'
